package com.mevi.lasheslam.domain.model

/** Estado de la notificación de aceptación de un curso. */
enum class CourseNotificationStatus(val value: String) {
    CREATED("created"),
    NOT_CREATED("notCreated");
}
